import type { ViewStyle, TextStyle } from 'react-native';

export type GlobalStyles = {
    mainContainer: ViewStyle;
    title: TextStyle;
    widgetContainer: ViewStyle;
    widget: ViewStyle;
    widgetNavContainer: ViewStyle;
    widgetNavTab: ViewStyle;
    widgetNavActiveTab: ViewStyle;
    widgetTitle: TextStyle;
    widgetSubTitle: TextStyle;
    calendarNav: ViewStyle;
    arrow: TextStyle;
    weekText: TextStyle;
    widgetRecap: TextStyle;
  };

export type IndexStyles = {
  chartSection: ViewStyle;
  calendarSection: ViewStyle;
  loginWrapper: ViewStyle;
};